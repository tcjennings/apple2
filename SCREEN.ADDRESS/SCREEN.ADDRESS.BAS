5 GOSUB 950
10 GOSUB 500
20 X = 0: Y = 0
30 INPUT "LINE NUMBER   (0-INDEXED)? ";Y
35 INPUT "COLUMN NUMBER (0-INDEXED)? ";X
40 IF Y < 0 OR Y > 23 OR X < 0 OR X > 39 THEN 10
45 POKE 254,Y : POKE 255,X : CALL 32768 : GOSUB 500
46 SA = PEEK(31) * 256 : SA = SA + PEEK(30)
50 AG = PEEK(251):AD = PEEK(252): SUM = PEEK(253)
51 MSB = PEEK(31):LSB=PEEK(30)
60 PRINT "SCREEN LINE ";Y;" COLUMN ";X
70 PRINT "SCREEN ADDRESS = ";SA;" (LSB:";LSB;", MSB:";MSB;")"
75 PRINT
76 PRINT "AUGEND:";AG;",ADDEND:";AD;" , SUM:";SUM
80 POKE SA,42
90 CALL 64780
100 GOTO 10
200 END
500 TEXT : HOME : VTAB 20 : RETURN
950 REM ML PROGRAM LOADER
960 PRINT CHR$(4)"BLOAD SCREEN.ADDRESS.ML"
999 RETURN

